Проект построен на FastAPI с использованием шаблонизатора Jinja2

Требования:

Python 3.10


Установка и запуск (Пишем в cmd VSкода):

python -m venv myvenv

myvenv\Scripts\activate

pip install -r requirements.txt

uvicorn main:app --reload